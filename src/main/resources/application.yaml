server:
port: 8080
spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/demo
    username: ${JDBC_USER}
    password: ${JDBC_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

jwt:
  secret: zavyalovegorsergeevich1BIB22066
  expiration: 3600000